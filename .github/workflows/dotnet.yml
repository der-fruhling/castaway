name: .NET

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      allow-publish:
        description: Allow Publishing
        required: true
        default: 'false'
      disable-ubuntu:
        description: Disable Ubuntu Build and Test
        required: false
        default: 'false'
      disable-mac:
        description: Disable Mac Build and Test
        required: false
        default: 'false'
      disable-windows:
        description: Disable Windows Build and Test
        required: false
        default: 'false'
jobs:
  build:
    env:
      disable: ${{ github.event.inputs.disable-ubuntu }}
    name: Ubuntu Build and Test
    runs-on: ubuntu-latest
    if: env.disable != 'true'
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build -c Debug --no-restore --nologo -clp:NoSummary
    - name: Test
      run: dotnet test Castaway.Base.Tests -c Debug --verbosity normal --no-build --nologo -clp:NoSummary
    - name: Upload Binaries
      uses: actions/upload-artifact@v2
      if: github.event.inputs.allow-publish == 'true'
      with:
        name: packages
        path: ./Castaway.*/bin/Debug/*.nupkg
  build-mac:
    env:
      disable: ${{ github.event.inputs.disable-mac }}
    name: Mac Build and Test
    runs-on: macos-latest
    if: env.disable != 'true'
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build -c Debug --no-restore --nologo -clp:"NoSummary;ErrorsOnly"
    - name: Test
      run: dotnet test Castaway.Base.Tests -c Debug --verbosity normal --no-build --nologo -clp:"NoSummary;ErrorsOnly"
  build-windows:
    env:
      disable: ${{ github.event.inputs.disable-windows }}
    name: Windows Build and Test
    runs-on: windows-latest
    if: env.disable != 'true'
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build -c Debug --no-restore --nologo -clp:"NoSummary;ErrorsOnly"
    - name: Test
      run: dotnet test Castaway.Base.Tests -c Debug --verbosity normal --no-build --nologo -clp:"NoSummary;ErrorsOnly"
  publish:
    env:
      disable-ubuntu: ${{ github.event.inputs.disable-ubuntu }}
      disable-mac: ${{ github.event.inputs.disable-mac }}
      disable-windows: ${{ github.event.inputs.disable-windows }}
    name: Publish
    runs-on: ubuntu-latest
    needs: [build, build-mac, build-windows]
    if: github.event.inputs.allow-publish == 'true' && env.disable-ubuntu != 'true' && env.disable-windows != 'true' && env.disable-mac != 'true'
    steps:
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 5.0.x
      - name: Download
        uses: actions/download-artifact@v2
        with:
          name: packages
      - name: Publish
        if: github.event.inputs.allow-publish == 'true'
        run: |
          dotnet nuget add source --username LiamCoal --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text --name github https://nuget.pkg.github.com/LiamCoalStudio/index.json
          dotnet nuget push Castaway.Level/bin/Debug/Castaway.Level.*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source github
          dotnet nuget push Castaway.OpenGL/bin/Debug/Castaway.OpenGL.*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source github
          dotnet nuget push Castaway.Math/bin/Debug/Castaway.Math.*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source github
          dotnet nuget push Castaway.Assets/bin/Debug/Castaway.Assets.*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source github
          dotnet nuget push Castaway.Rendering/bin/Debug/Castaway.Rendering.*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source github
          dotnet nuget push Castaway.OpenGL.Native/bin/Debug/Castaway.OpenGL.Native.*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source github
